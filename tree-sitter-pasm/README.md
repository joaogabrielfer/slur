# tree-sitter-pasm

Tree-sitter grammar and highlight queries for `pasm`.

## Generate Parser

```bash
cd tree-sitter-pasm
npm install
npx tree-sitter generate
```

Neovim can also generate/build it through `nvim-treesitter` when configured with this local parser path.
